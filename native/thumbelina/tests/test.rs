#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }

    // #[test]
    // fn it_reads_image_binary_in_memory() {
    //     assert_eq!(img_buffer.to_owned(), img.as_bytes());
    // }
}
