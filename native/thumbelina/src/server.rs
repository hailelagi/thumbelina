

// fn process(operation:: Operation, pid: Pid) {
//     tokio::spawn(async move {
//         match operation {
//            Operation::Resize => operation::try_resize(extension, buffer, width as u32, height as u32),
//         Operation::Thumbnail => operation::thumbnail(extension, buffer, width as u32, height as u32)
//         Operation::FlipHorizontal => operation::flip(env, binaries, extension, Direction::Horizontal, pid)
//         Operation::FlipVertical => operation::flip(env, binaries, extension, Direction::Vertical, pid)
//         Operation::Rotate => operation::rotate(env, binaries, extension, width as i32, pid),
//         Operation::Blur => operation::blur(env, binaries, extension, width as f32, pid),
//         Operation::Brighten => operation::brighten(env, binaries, extension, width as i32, pid),
//         Operation::Greyscale => operation::greyscale(env, binaries, extension, pid),
//         }
// }
//     );


// }
